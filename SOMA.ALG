Algoritmo "soma"


// Descricao : Fazer um programa para ler dois valores inteiros X e Y, e depoiis mostrar na tela o valor da soma destes numeros.

Var

    x, y, soma : inteiro
    

Inicio

    escreva ("Digite o valor de x: ")
    leia (x)
    
    escreva ("Digite o valor de y: ")
    leia (y)
    
    soma <- x+y
    escreval ("Soma = ", soma)

Fimalgoritmo